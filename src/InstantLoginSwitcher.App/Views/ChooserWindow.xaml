<Window x:Class="InstantLoginSwitcher.App.Views.ChooserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose User"
        Height="420"
        Width="560"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ResizeMode="CanMinimize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="16" FontWeight="SemiBold" Text="Choose the user to sign in" Margin="4" />

        <ListBox x:Name="TargetsList"
                 Grid.Row="1"
                 Margin="0,8,0,8"
                 ItemsSource="{Binding}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="2" BorderBrush="#FFD7D7D7" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Width="48" Height="48" CornerRadius="24" Background="#FFE7EEF6" VerticalAlignment="Center">
                                <Image Source="{Binding PicturePath}" Stretch="Fill" />
                            </Border>

                            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock FontWeight="SemiBold" Text="{Binding FullName}" />
                                <TextBlock Text="{Binding Qualified}" Opacity="0.8" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="100" Height="30" Margin="0,0,8,0" Content="Cancel" Click="Cancel_Click" />
            <Button Width="100" Height="30" Content="Switch" Click="Switch_Click" />
        </StackPanel>
    </Grid>
</Window>
