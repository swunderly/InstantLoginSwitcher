<Window x:Class="InstantLoginSwitcher.App.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Password"
        Height="200"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" TextWrapping="Wrap" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,12,0,12" Height="30" VerticalContentAlignment="Center" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Height="30" Margin="0,0,8,0" Content="Cancel" Click="Cancel_Click" />
            <Button Width="90" Height="30" Content="OK" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
